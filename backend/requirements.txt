# Minimum versions current as of mid-2026 (unpinned upper bound: fresh installs get latest)
fastapi>=0.115
uvicorn[standard]>=0.32
psutil>=6.0
zeroconf>=0.140    # announces http://doge.local over mDNS while the server runs
# Scrypt PoW uses stdlib hashlib.scrypt (OpenSSL) — no extra dependency needed.
# GPU mining extras live in requirements-gpu.txt (optional; CPU works without them).
